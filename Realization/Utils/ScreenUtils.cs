namespace Realization.Utils
{
    public static class ScreenUtils
    {
        public const float DefaultWidth = 1920;
        public const float DefaultHeight = 1080f;
        public const float DefaultAspect = (DefaultWidth) / DefaultHeight;
    }
}